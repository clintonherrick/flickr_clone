<%= form_for [@image_attachment, @comment] do |f| %>
   <div class="field">
     <%= f.label :user_id %><br>
     <%= f.text_field :user_id %>
     <%= f.label :user_comment %><br>
     <%= f.text_field :user_comment %>
   </div>

   <div class="actions">
     <%= f.submit %>
   </div>
<% end %>
